Prospectus.extra_dep('file', 'prospectus_circleci')

item do
  noop

  deps do
    item do
      name 'gems'

      expected do
        static
        set 'green'
      end

      actual do
        gemnasium
        slug 'dock0/scheduled_build'
      end
    end
  end

  extend ProspectusCircleci::Build.new('dock0/scheduled_build')
end
